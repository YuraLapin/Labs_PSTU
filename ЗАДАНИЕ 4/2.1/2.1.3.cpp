#include <iostream>

using namespace std;

int main()
{
    const int a = 10; 
    int b;
    int c;
    // Три переменные для вывода трёх чисел на экран

    b = 20;
    // Присваиваем значение переменной b

    c = 20;
    с--;
    // Присвовиваем переменой с значение 19 через декремент

    cout << a << endl << b << endl << с;
    // Непосредственно вывод трёх значений на экран

    return 0;
}